// Top-level game shell. Boots into the main menu, hosts active game mode.

export function Game() {
  return (
    <div style={{ position: "absolute", inset: 0, color: "#3a2a14", display: "grid", placeItems: "center", fontSize: 32 }}>
      Letra is loading…
    </div>
  );
}
